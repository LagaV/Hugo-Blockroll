# Contributing

Issues and pull requests are welcome. Keep the OPML renderer compatible with standard OPML 2.0 readers and treat `blogroll-*` fields as optional extensions.

Run `scripts/check.sh` before submitting changes.
